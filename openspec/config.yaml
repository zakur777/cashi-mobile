strict_tdd: true
context: |
  cashi-mobile is a Node.js/TypeScript, React project.
  Detected markers: package.json, tsconfig.json, npm package manager.
  Package managers: npm.
  Additional evidence: Test-like files detected (14); examples: .pi\agents\sdd-spec.md, __tests__\CategoryList.test.tsx, __tests__\TransactionList.test.tsx, __tests__\balanceScreen.test.tsx, __tests__\categoriesStorage.test.ts.
  Primary test command: npm test.
  Unit tests: Jest (npm test).
  Integration tests: none.
  E2E tests: none.
rules:
  proposal:
    require_problem_statement: true
  spec:
    require_acceptance_criteria: true
  design:
    require_tradeoffs: true
  tasks:
    protect_review_workload: true
  apply:
    test_command: "npm test"
  verify:
    test_command: "npm test"
testing:
  detected: "2026-05-17"
  runner:
    command: "npm test"
    framework: "Jest"
  layers:
    unit: "Jest"
    integration: ""
    e2e: ""
  commands:
    unit:
      - scope: "."
        command: "npm test"
        framework: "Jest"
    integration: []
    e2e: []
  coverage:
    command: "npm run test:coverage"
    commands:
      - scope: "."
        command: "npm run test:coverage"
        framework: "Jest"
quality:
  lint: ""
  lint_commands: []
  typecheck: "npm run typecheck"
  typecheck_commands:
    - scope: "."
      command: "npm run typecheck"
      framework: "TypeScript"
  format: ""
  format_commands: []
